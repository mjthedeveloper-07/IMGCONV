[build.environment]
PYTHON_VERSION = "3.11.5"  # Or another valid version

[build]
  functions = "functions"
  publish = "static"
  command = "pip install -r requirements.txt"

[functions]
  directory = "functions"
