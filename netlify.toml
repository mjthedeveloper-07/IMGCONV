[build.environment]
PYTHON_VERSION = "3.13.0"  # Update to a valid version

[build]
  functions = "functions"
  publish = "static"
  command = "pip install -r requirements.txt"

[functions]
  directory = "functions"
  
